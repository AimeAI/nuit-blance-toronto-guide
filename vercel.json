{
  "buildCommand": "cd nuit-blanche-app && npm install && npm run build",
  "outputDirectory": "nuit-blanche-app/dist",
  "installCommand": "cd nuit-blanche-app && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
